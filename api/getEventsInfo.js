module.exports.handler = async (event) => {
  return {
    statusCode: 200,
    body: JSON.stringify({
      message: "Getting all 3 wedding events information",
    }),
  };

  callback(null, response);
};
